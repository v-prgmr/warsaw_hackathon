diff --git a/lens-studio/Assets/Scripts/App/Registration/RegistrationPresentation.ts b/lens-studio/Assets/Scripts/App/Registration/RegistrationPresentation.ts
index 6b3e7f1..5926f6b 100644
--- a/lens-studio/Assets/Scripts/App/Registration/RegistrationPresentation.ts
+++ b/lens-studio/Assets/Scripts/App/Registration/RegistrationPresentation.ts
@@ -20,9 +20,9 @@ export const REGISTRATION_STEP_TITLES: Record<RegistrationStep, string> = {
 };
 
 export const REGISTRATION_STEP_DESCRIPTIONS: Record<RegistrationStep, string> = {
-  startRobot: "Power on your robot.\nRun ./launcher/scripts/start.sh on your Mac.",
+  startRobot: "Start the robot stack and g1_ar_bridge\non the robot laptop (Ubuntu).",
   connectBridge:
-    "Enter your Mac's IP.\nUse same Wi‑Fi for robot, Mac, and Spectacles.",
+    "Enter the robot laptop's Wi‑Fi IP.\nSame Wi‑Fi for the laptop and Spectacles.",
   registerRobot: "",
 };
 
@@ -181,7 +181,7 @@ export function projectRegistrationPresentation(
       panelStatusText = NO_RESPONSE_STATUS_MSG;
       panelStatusColor = COLOR_ERROR;
       panelDetailText =
-        "Check that ./launcher/scripts/start.sh is running, then retry or switch to Manual pose";
+        "Check that g1_ar_bridge is running on the laptop, then retry or switch to Manual pose";
     } else if (showTagScanOverlay) {
       panelStatusText = tagStatus.text;
       panelStatusColor = tagStatus.color;
@@ -261,7 +261,7 @@ export function registrationStepTitle(mode: RegistrationMode): string {
 }
 
 export function buildAprilTagDescription(displayName: string): string {
-  return `Look at the tag on the ${displayName}.`;
+  return `Look at the AprilTag on the wall and step sideways. The ${displayName} sees it too.`;
 }
 
 export const REGISTRATION_DESCRIPTION_MANUAL =
